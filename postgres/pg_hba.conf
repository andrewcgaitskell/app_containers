# TYPE  DATABASE        USER            ADDRESS                 METHOD
# 1. Allow local system/Unix domain socket connections (essential for internal tools)
local   all             all                                     scram-sha-256
# 2. Allow IPv4 loopback connections (localhost)
host    all             all             127.0.0.1/32            scram-sha-256
# 3. Allow IPv6 loopback connections (localhost)
host    all             all             ::1/128                 scram-sha-256
